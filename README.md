## 🚀 Getting Started

Follow the steps below to run the project locally or in production mode.

### Step 1: Clone the repository
```bash
git clone https://github.com/Rupesh93/purity-ui-dashboard.git


### Step 2: Checkout the main branch
git checkout main

### Step 3: Run in production mode (optional)

If you want to run the project in production mode, use:
npm run prod


### Step 4: Install dependencies (for local development)

If you want to run the project locally, first install dependencies:
npm install

### Step 5: Start the development server    
npm run dev

🎉 Open in Browser

Once the server is running, open:
http://localhost:3000
